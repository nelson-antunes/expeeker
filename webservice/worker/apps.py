from __future__ import unicode_literals

from django.apps import AppConfig

class WorkerShowConfig(AppConfig):
	name = 'worker_show'

class WorkerListConfig(AppConfig):
    name = 'worker_list'

class WorkerAddConfig(AppConfig):
    name = 'worker_add'
